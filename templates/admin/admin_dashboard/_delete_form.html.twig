<form method="post" action="{{ path('app_admin_dashboard_delete', {'id': admin_dashboard.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ admin_dashboard.id) }}">
    <button class="btn">Supprimer</button>
</form>
